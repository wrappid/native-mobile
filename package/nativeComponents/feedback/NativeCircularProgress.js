import React from 'react';
import {SCCircularProgress} from '../../feedback/SCCircularProgress';

export default function NativeCircularProgress() {
  return <SCCircularProgress animation={true} />;
}
